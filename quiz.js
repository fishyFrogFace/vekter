const questions = 20

function random(max) {
	return Math.floor((Math.random() * max) + 1)
}

function alcohol() {
	const elements = getRandomElements(alkohol)
	alert("Alkohol!")
}

function getRandomElements(amount, list) {
	return undefined
}